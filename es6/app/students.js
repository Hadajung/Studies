export const students = ["Harry", "Ron", "Hermione"];
export const total = 100;

const add = (x, y) => {
  return x + y;
}

const multiply = (x, y) => {
  return x * y;
}

export { add, multiply };
export default multiply;
